    // let = block_select_1 =  Math.floor(Math.random() * 3);
		// console
		// function draw() {

  			// var mmm = document.getElementById("canvas")
			// var ctx = mmm.getContext('2d');

  			// // 패턴으로 사용할 이미지 오브젝트를 생성한다
  			// var img = new Image();
  			// img.src = 'C:\Users\김성진\Desktop\tutorial\sunjin_tutorial\image2.png';

  			// img.onload = function() {

    		// // 패턴을 생성한다
    		// 	var ptrn = ctx.createPattern(img,'repeat');
    		// 	ctx.fillStyle = ptrn;
    		// 	ctx.fillRect(0,0,150,150);
			

  		// 	}
		// }

		// draw();


        //context.clearRect(0, 0, canvas.width, canvas.height);
			
			
            // context.beginPath();
            // context.arc(x, y, 8, 0, Math.PI * 2, false);
          // context2.arc(a, b, 8, 0, Math.PI * 2, false)
            // context.closePath();
            // context.fill();
            // x = 10이고 width가 300이다.
          // 1초에 60번 찍히니깐 x = 60이 될 때가 1초에 해당함
          // 5초에 x == 300
          // y값 붙어서 출력하려면 20씩증가 해야함
          //반지름이 10이니 2*pi*r 하면 지름이 20*15 = 300
          // 각 칸의 크기는 20x20이다
            // context.arc(x, 10, 8, 0, Math.PI *2, false);
          // context.arc(x, 30, 8, 0, Math.PI * 2, false);
          // context.arc(x, 50, 8, 0, Math.PI * 2, false);
          // context.arc(x, 70, 8, 0, Math.PI * 2, false);
          // context.arc(x, 90, 8, 0, Math.PI * 2, false);
          // context.arc(x, 110, 8, 0, Math.PI * 2, false);
          // context.arc(x, 130, 10, 0, Math.PI * 2, false);
          // context.arc(x, 150, 10, 0, Math.PI * 2, false);
          // context.arc(x, 170, 10, 0, Math.PI * 2, false);
          // context.arc(x, 190, 10, 0, Math.PI * 2, false);
          // context.arc(x, 210, 10, 0, Math.PI * 2, false);
          // context.arc(x, 230, 10, 0, Math.PI * 2, false);
          // context.arc(x, 250, 10, 0, Math.PI * 2, false);
          // context.arc(x, 270, 10, 0, Math.PI * 2, false);
          // context.arc(x, 290, 10, 0, Math.PI * 2, false);